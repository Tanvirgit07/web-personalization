import { describe, expect, it } from "vitest";

describe("web-personalization", () => {
  it("should have the correct package name", () => {
    const packageName = "web-personalization";

    expect(packageName).toBe("web-personalization");
  });
});
